_format_version: "3.0"
services:
  - name: account-details-service
    url: https://account-details-service:8082
    #tls_verify: false  # Disable SSL verification for self-signed certs
    routes:
      - name: details
        hosts:
          - localhost
        paths:
          - ~/account-details(|/.*)
        strip_path: false
        protocols:
          - https
        methods:
          - GET
          - POST
          - PUT
          - DELETE

  - name: account-address-service
    url: https://account-address-service:8084
    #tls_verify: false  # Disable SSL verification for self-signed certs
    routes:
      - name: address
        hosts:
          - localhost
        paths:
          - ~/account-address(|/.*)
        strip_path: false
        protocols:
          - https
        methods:
          - GET
          - POST
          - PUT
          - DELETE

  - name: account-balance-service
    url: https://account-balance-service:8086
    #tls_verify: false  # Disable SSL verification for self-signed certs
    routes:
      - name: balance
        hosts:
          - localhost
        paths:
          - ~/account-balance(|/.*)
        strip_path: false
        protocols:
          - https
        methods:
          - GET
          - POST
          - PUT
          - DELETE

  - name: ais-composite-service
    url: https://ais-composite-service:8088
    tls_verify: false  # Disable SSL verification for self-signed certs
    routes:
      - name: composite
        hosts:
          - localhost
        paths:
          - ~/ais-composite(|/.*)
        strip_path: false
        protocols:
          - https
        methods:
          - GET
          - POST
          - PUT
          - DELETE